# Force npm to use compatible dependencies
legacy-peer-deps=true
# Include optional dependencies for Rollup
include=optional
# Use npm instead of yarn
engine-strict=false
# Force skip native modules for rollup
rollup-skip-native=true
